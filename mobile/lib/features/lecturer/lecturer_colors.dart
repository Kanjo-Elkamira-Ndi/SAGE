import '../student/student_colors.dart';

/// Lecturer token set. The lecturer app shares the SAGE design system with the
/// student app, so this is an alias of the same Stitch tokens.
typedef LecturerColors = StudentColors;
